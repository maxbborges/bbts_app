import 'package:bbts_flutter/models/funcionario.dart';
import 'package:bbts_flutter/providers/db_provider.dart';
import 'package:flutter/services.dart' show rootBundle;
import 'dart:convert';

class CarApiProvider {
  // Future<List<Funcionario>> getAllCars() async {
  //   return 0;
    // String jsonString = await rootBundle.loadString('assets/funcionarios.json');
    // return json.decode(jsonString).forEach((car) {
    //   print(car);
    //   DBProvider.db.CreateEmployee(Funcionario.fromJson(car));
    // });
  // }
}
